---
description: File a blocking bug report with reproduction steps
argument-hint: <title> --steps <repro_steps> [--evidence <text>]
allowed-tools: Bash
---

File a bug report that's blocking progress.

Run:
```bash
blackboard bug-report $ARGUMENTS
```
