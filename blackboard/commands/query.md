---
description: Run an ad-hoc SQL query against the blackboard database
argument-hint: <sql>
allowed-tools: Bash
---

Run an ad-hoc SQL query against the blackboard.

Run:
```bash
blackboard query "$ARGUMENTS"
```
