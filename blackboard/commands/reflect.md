---
description: Capture a reflection on the current session or plan
argument-hint: [content] [--trigger <manual|compact|completion|stop>]
allowed-tools: Bash
---

Capture insights and reflections about the current work.

Run:
```bash
blackboard reflect $ARGUMENTS
```

If no content provided, the command will prompt for it interactively.
